# Migration Checklist

1. Copy existing HTML files to repository root.
2. Copy existing CSS into css/.
3. Copy existing JS into js/.
4. Preserve Archive/.
5. Copy configuration files into config/.
6. Copy API files into api/.
7. Verify application runs unchanged.
8. Commit as Version 0.1 Repository Baseline.
